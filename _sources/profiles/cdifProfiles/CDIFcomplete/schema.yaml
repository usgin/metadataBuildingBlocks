---
$schema: https://json-schema.org/draft/2020-12/schema
type: object
title: CDIF complete metadata schema
description: 'JSON schema for JSON-LD documents that describe science datasets for the CDIF
  complete profile, combining discovery metadata with data description, distribution
  (DataDownload, archive, WebAPI), and provenance extensions. Composes cdifCore,
  cdifOptional, cdifDataDescription, cdifArchiveDistribution, and cdifProvenance building blocks.'
allOf:
- $ref: ../../../cdifProperties/cdifCore/schema.yaml
- $ref: ../../../cdifProperties/cdifOptional/schema.yaml
- $ref: ../../../cdifProperties/cdifDataDescription/schema.yaml
- $ref: ../../../cdifProperties/cdifArchiveDistribution/schema.yaml
- $ref: ../../../cdifProperties/cdifProvenance/schema.yaml
